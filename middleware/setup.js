import { isNode } from 'browser-or-node'

export default function({ store, route, redirect }) {
  /*if (!isNode) {
    if (store.getters['swarm/nameByID'](app.node.ipfs.id.id)) {
      return redirect('/profil')
    }
  }*/
}
